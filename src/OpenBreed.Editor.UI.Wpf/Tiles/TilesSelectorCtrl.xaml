<UserControl x:Class="OpenBreed.Editor.UI.Wpf.Tiles.TilesSelectorCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenBreed.Editor.UI.Wpf.Tiles"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ComboBox Grid.Row="0"
                  Grid.Column="0"
                  Height="22"
                  Margin="10,0,10,0"
                  ItemsSource="{Binding TileSetIds}"
                  SelectedItem="{Binding CurrentTileSetId}"/>
        <local:TileSetViewerCtrl Grid.Row="1"
                                 Grid.Column="0"
                                 Margin="10,0,10,0" 
                                 DataContext="{Binding Viewer}" />
    </Grid>
</UserControl>
