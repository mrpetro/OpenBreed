<UserControl x:Class="OpenBreed.Editor.UI.Wpf.Animations.AnimationCurvesEditorCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenBreed.Editor.UI.Wpf.Animations"
             xmlns:Wpf="clr-namespace:OpenBreed.Editor.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../CommonResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="26" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <local:AnimationCurvesEditorToolsCtrl Grid.Row="0" HorizontalAlignment="Left" DataContext="{Binding Tools}" />
        <Wpf:GLWpfControlEx Grid.Row="1" InitFunc="{Binding InitFunc}" >
            <Wpf:GLWpfControlEx.ContextMenu>
                <ContextMenu ItemsSource="{Binding Actions}"
                             IsOpen="{Binding ActionsShow}" >
                    <ContextMenu.ItemTemplate  >
                        <DataTemplate DataType="MenuItem">
                            <MenuItem Command="{Binding Command}" >
                                <MenuItem.Template>
                                    <ControlTemplate>
                                        <Label Content="{Binding HeaderText}" />
                                    </ControlTemplate>
                                </MenuItem.Template>
                            </MenuItem>
                        </DataTemplate>
                    </ContextMenu.ItemTemplate>
                </ContextMenu>
            </Wpf:GLWpfControlEx.ContextMenu>
        </Wpf:GLWpfControlEx>
    </Grid>
</UserControl>
