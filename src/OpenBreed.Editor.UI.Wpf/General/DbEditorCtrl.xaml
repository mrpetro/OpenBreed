<UserControl xmlns:Logging="clr-namespace:OpenBreed.Editor.UI.Wpf.Logging"  x:Class="OpenBreed.Editor.UI.Wpf.General.DbEditorCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenBreed.Editor.UI.Wpf.General"
             xmlns:localEntityClasses="clr-namespace:OpenBreed.Editor.UI.Wpf.EntityClasses"
             xmlns:vm="clr-namespace:OpenBreed.Editor.VM.Database;assembly=OpenBreed.Editor.VM"
             xmlns:vmEntityClasses="clr-namespace:OpenBreed.Editor.VM.EntityClasses;assembly=OpenBreed.Editor.VM"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="VisibleIfTrueConverter" />
        <DataTemplate DataType="{x:Type vm:DbTablesEditorMainVM}">
            <local:DbTablesEditorMainCtrl/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmEntityClasses:EntityClassesTreeEditorVM}">
            <localEntityClasses:EntityClassesTreeEditorCtrl/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ContentControl Content="{Binding CurrentView}" />
    </Grid>
</UserControl>
